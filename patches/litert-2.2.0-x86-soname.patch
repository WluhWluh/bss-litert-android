diff --git a/litert/kotlin/BUILD b/litert/kotlin/BUILD
index 2ef539f..d0dd294 100644
--- a/litert/kotlin/BUILD
+++ b/litert/kotlin/BUILD
@@ -182,6 +182,7 @@ cc_library(
 # copybara:comment_begin(oss-only)
 tflite_jni_binary(
     name = "litert_jni",
+    soname = "liblitert_jni.so",
     linkopts = tflite_jni_linkopts() + tflite_linkopts_no_undefined() + litert_android_linkopts(),
     linkscript = ":litert_api_version_script.lds",
     # Set stamp = 0 to avoid including internal info to the binary for the public release.
@@ -201,6 +202,7 @@ cc_library(
 
 tflite_jni_binary(
     name = "LiteRt",
+    soname = "libLiteRt.so",
     linkopts = tflite_jni_linkopts() + tflite_linkopts_no_undefined() + litert_android_linkopts(),
     linkscript = ":litert_version_script.lds",
     # Set stamp = 0 to avoid including internal info to the binary for the public release.
diff --git a/tflite/build_def.bzl b/tflite/build_def.bzl
index dbf773d..027c2b7 100644
--- a/tflite/build_def.bzl
+++ b/tflite/build_def.bzl
@@ -239,6 +239,7 @@ def tflite_exec_properties(memory = "20g"):
 
 def tflite_jni_binary(
         name,
+        soname = None,
         copts = tflite_copts(),
         linkopts = tflite_jni_linkopts(),
         linkscript = LINKER_SCRIPT,
@@ -263,7 +264,7 @@ def tflite_jni_binary(
         "//conditions:default": [
             "-Wl,--version-script,$(location {})".format(linkscript),
             "-Wl,--undefined-version",
-            "-Wl,-soname," + name,
+            "-Wl,-soname," + (soname or name),
         ],
     })
     cc_binary(
