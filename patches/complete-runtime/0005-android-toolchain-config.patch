diff --git a/.bazelrc b/.bazelrc
--- a/.bazelrc
+++ b/.bazelrc
@@ -151,21 +151,27 @@ build:android --host_crosstool_top=@bazel_tools//tools/cpp:toolchain
 build:android_arm --config=android
 build:android_arm --cpu=armeabi-v7a
 build:android_arm --fat_apk_cpu=armeabi-v7a
 build:android_arm --platforms=@org_tensorflow//tensorflow/tools/toolchains/android:armeabi-v7a
+build:android_arm --incompatible_enable_cc_toolchain_resolution
+build:android_arm --incompatible_enable_android_toolchain_resolution
 build:android_arm64 --config=android
 build:android_arm64 --cpu=arm64-v8a
 build:android_arm64 --fat_apk_cpu=arm64-v8a
 build:android_arm64 --platforms=@org_tensorflow//tensorflow/tools/toolchains/android:arm64-v8a
 build:android_arm64 --incompatible_enable_cc_toolchain_resolution
 build:android_arm64 --incompatible_enable_android_toolchain_resolution
 build:android_x86 --config=android
 build:android_x86 --cpu=x86
 build:android_x86 --fat_apk_cpu=x86
 build:android_x86 --platforms=@org_tensorflow//tensorflow/tools/toolchains/android:x86
+build:android_x86 --incompatible_enable_cc_toolchain_resolution
+build:android_x86 --incompatible_enable_android_toolchain_resolution
 build:android_x86_64 --config=android
 build:android_x86_64 --cpu=x86_64
 build:android_x86_64 --fat_apk_cpu=x86_64
 build:android_x86_64 --platforms=@org_tensorflow//tensorflow/tools/toolchains/android:x86_64
+build:android_x86_64 --incompatible_enable_cc_toolchain_resolution
+build:android_x86_64 --incompatible_enable_android_toolchain_resolution
 
 # Build everything statically for Android since all static libs are later
 # bundled together into a single .so for deployment.
diff --git a/WORKSPACE b/WORKSPACE
--- a/WORKSPACE
+++ b/WORKSPACE
@@ -396,8 +396,11 @@ exynos_ai_litecore()
 # Android rules. Need latest rules_android_ndk to use NDK 26+.
 load("@rules_android_ndk//:rules.bzl", "android_ndk_repository")
 
-android_ndk_repository(name = "androidndk")
+android_ndk_repository(
+    name = "androidndk",
+    api_level = 23,
+)
 
 load("//:android_ndk_env.bzl", "check_android_ndk_env")
 
 check_android_ndk_env(name = "android_ndk_env")
